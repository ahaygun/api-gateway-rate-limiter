# Prometheus scrape config for the gateway observability demo.
# Scrapes the gateway's /metrics endpoint frequently so the Grafana panels
# stay lively during a short local recording.
global:
  scrape_interval: 2s
  evaluation_interval: 2s

scrape_configs:
  - job_name: gateway
    static_configs:
      - targets: ["gateway:8080"]
